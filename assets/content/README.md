# Starter Assets

The assets.json is imported, using the raw asset path and version.

`Card Name` and `Version` are used for deduplication.

Older versions are not cleaned up since there is no hash check to ensure the user had not modified them.
